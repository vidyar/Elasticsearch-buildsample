language: python

#python:
 
services:
 - elasticsearch

install: pip install --use-mirrors -r requirements.txt

script:
 - python sample.py

#exclude
branches:
  except:
    - stage
    - prod
    
#archive: true
