language: python
python:
# - "3.3"
 - "2.7"
services: 
 - elasticsearch
install: pip install --use-mirrors -r requirements.txt
script:
 - python sample.py
notifications:
  email:
    - exampleone@org.com
