language: python
python:
 - "2.7"
 #- "2.6"
services:
 - elasticsearch
install: pip install --use-mirrors -r requirements.txt
script:
 - python sample.py
#include
branches:
  except:
    - stage
    - prod
